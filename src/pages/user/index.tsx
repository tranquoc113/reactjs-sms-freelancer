import List, { PATH as LIST } from './UserList';
import Setting, { PATH as SETTING } from './UserSetting';
import Detail, { PATH as DETAIL } from './UserDetail';

export default { List, Setting, Detail };
export const PATH = { LIST, SETTING, DETAIL };
